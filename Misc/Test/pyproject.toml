[project]
name = "dist_blob2dgp"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "pandas",
    "xarray",
    "xbout",
    "boututils",
    "scikit-learn",
    "matplotlib",
    "pyqt6",
    "tqdm",
]

[project.scripts]
script_name = "package.module:function"
